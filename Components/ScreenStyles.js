import { StyleSheet } from "react-native";

const uploadStyle = StyleSheet.create({
  uploadView: {
    justifyContent: "center",
    alignItems: "center",
    height: "90%",
    width: "100%",
    gap: 10,
  },
  uploadPressable: {
    backgroundColor: "red",
    padding: 10,
    borderRadius: 10,
  },
});

export { uploadStyle };
